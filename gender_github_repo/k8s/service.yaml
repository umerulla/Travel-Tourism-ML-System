apiVersion: v1
kind: Service
metadata:
  name: gender-api-service
spec:
  type: NodePort
  selector:
    app: gender-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30037
